apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: library
  namespace: immich
  labels:
    pvc-plumber.io/enabled:
    pvc-plumber.io/manage-volsync:
    pvc-plumber.io/tier:
    restore-policy:
  annotations:
    argocd.argoproj.io/compare-options:
spec:
  # Local LVM block (default vanillax-local-rwo). Was briefly TrueNAS NFS, but
  # the csi.truenas.io driver crash-loops on this SNO — keeping ALL storage
  # node-local removes any TrueNAS dependency. Lab box; recreate-fresh.
  dataSourceRef:

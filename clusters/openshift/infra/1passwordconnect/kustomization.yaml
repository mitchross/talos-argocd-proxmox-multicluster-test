apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - "../../../../manifests/infra/1passwordconnect/base"
  # Grant nonroot-v2 so the chart runs as its designed non-root UID on
  # OpenShift (no UID stripping / HOME surgery needed). See the file's comment.
  - scc-rolebinding.yaml

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - "../../../../manifests/infra/cert-manager/base"
  - scc-rolebinding.yaml
patches:
  # OpenShift uses the homelab-lab cloudflare token (covers vanillax.xyz).
  - path: patches/externalsecret-cloudflare-lab.yaml
